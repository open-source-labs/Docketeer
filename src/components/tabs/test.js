// const fetchGitData = async () => {
//   let data = await 1 + 1;
//   // const jsonData = await data.json();
//   return data;
// }
// fetchGitData()
//   .then((res) => console.log(res));

// const activeContainers = {amazing_morse: true, container_2: true};


// const gitData = async () => {
//   // map over activeContainers keys
//   const keys = Object.keys(activeContainers);

//   console.log(keys);
//   // const data = Promise.all(keys.map((item) => fetchGitData()));
//   // let getData = Object.keys(activeContainers).map(container => {
//   //   const data = fetchGitData();
//   //   return data;
//   // });
//   // console.log(getData);
// }

// gitData();

//   // return(
//   //   <div><h1>{timePeriod}</h1> <p>{container}</p></div>
//   // )
//   // console.log(container);



//   const fetchGitData = async () => {
//     let data = await 1 + 1;
//     return data;
//   }
  
//   const activeContainers = {amazing_morse: true, container_2: true};
  
  
//   const gitData = async () => {
//     // map over activeContainers keys
//     const keys = Object.keys(activeContainers);
//     const data = Promise.all(keys.map(() => fetchGitData()));
//     return data.then((res) => setState(res));
//   }
  
//   gitData();

// console.log('hi')
// {state ? state.map((res) => JSX) : ''};

// React Render DOM
// useEffect -> Rerender React DOM w/ new State
